#include "main.h"
/**
 * _isalpha - used to check if the letter is lower or upper case
 * @c: is the charachter containing letters
 * Return: 1 if c is a letter lower or upper case, else 0
 */
int _isalpha(int c)
{
	if (c >= 65 && c <= 120)
	{
	return (1);
	}
return (0);
}
