#include "main.h"
/**
 * _abs - function get absolute value of integar
 * @a: return positive value
 * Return: the absolute value of the input
 */
int _abs(int a)
{

	if (a < 0)
	{
	return (a * -1);
	}
		else
		return (a);
}
