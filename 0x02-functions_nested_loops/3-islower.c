#include "main.h"
/**
 * _islower - it checks for a lower case
 * @c: checks that if the value is c print 1
 * Return: 0
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
	return (1);
	}
return (0);
}
