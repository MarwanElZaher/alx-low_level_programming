#include "main.h"
/**
 * _memcpy - a function that copies an area in the memory
 * @dest: the place where the memory is stored
 * @src: the place of memory that is going to be copied
 * @n: number of bytes to be copied.
 * Return: to dest where the copied data placed.
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
int c, n;

	for (c = 0 ; c < n ; c++)
	{
	dest[c] = src[r];
	n--;
	}
return (dest);
}
