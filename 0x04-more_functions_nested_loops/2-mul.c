#include "main.h"
/**
 * mul - function used to multiplty two integers
 * @a: first integer multiplied.
 * @b: second integer to be multiplied.
 * Return: result always 0 (means success).
 */
int mul(int a, int b)
{
int result;

	result = a * b;
return (result);
}
